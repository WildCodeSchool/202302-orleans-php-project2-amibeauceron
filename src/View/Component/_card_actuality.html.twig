<article>
	<header>
		<h2>{{ actuality.title|upper }}</h2>
	</header>
	<div class="news-content">
		{{ actuality.content|length > 150 ? actuality.content|slice(0, 150) ~ '...' : actuality.content }}
	</div>
	<footer>
		<div>
			<small>
				Date :{{ actuality.creation_date|date("d/m/Y : H:m") }}
			</small>
		</div>
		<div>
			<a href="/news/show?id={{ actuality.id }}" role="button" class="secondary">Détail</a>
		</div>
	</footer>
</article>
